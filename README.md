# Pita-flow
A reflow oven from a flat bread maker

See [`docs/firmware-plan.md`](docs/firmware-plan.md) for the firmware architecture and
[`firmware/`](firmware/) for the ESP-IDF firmware implementation (targeting the ESP32-C3 Super Mini).

