# Pita-flow
A reflow oven from a flat bread maker

See [`docs/firmware-plan.md`](docs/firmware-plan.md) for the firmware architecture and
[`firmware/`](firmware/) for the host-testable control core plus the in-progress
Wio Terminal adaptation (built-in screen/buttons, microSD YAML profile catalog,
and speaker notifications).
